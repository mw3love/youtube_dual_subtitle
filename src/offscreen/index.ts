// Offscreen document — hosts Chrome built-in Translator API calls.
// Future: receive messages from background, call window.Translator, return results.

console.log('[YDT] offscreen document loaded');
